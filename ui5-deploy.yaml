# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "2.4"
metadata:
  name: zvgt.hppm.delivery_create
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          destination: ERP_EMBEDDED_D91_100TEST
          url: http://vpavd910.vetropack.corporate.ads:44300
          auth: basic
          client: "100"
        credentials:
          username: env:UI5_USERNAME
          password: env:UI5_PASSWORD
        app:
          name: ZVGT_DEL_CREATE
          description: "HPPM: Delivery Create"
          package: ZVGT_HPPM_DELIVERY_CREATE
          transport: "D91K912362"
